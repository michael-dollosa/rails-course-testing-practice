<div id="page-content">
  <h1 class="text-center mt-4">All Blogs</h1>

  <div class="flickr_pagination">
     <%= will_paginate @articles, :container =>false %>
  </div>

  <%= render 'article', data: @articles %>

  <div class="flickr_pagination">
     <%= will_paginate @articles, :container =>false %>
  </div>
</div>